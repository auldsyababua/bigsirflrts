# Grafana Dashboard Provisioning for FLRTS Story 1.7
apiVersion: 1

providers:
  - name: 'FLRTS Dashboards'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards